
#include <iostream>
#include <fstream>

#include <bits/stdc++.h>
int main(int argc, char *argv[])
{
	if (argc != 4)
		return(1);
	std::ifstream MyFile(argv[1]);

	std::string line;
	std::string s1;
	s1 = argv[2];
	auto first = s1.begin();
	auto last = s1.end();
	
	while (getline(MyFile, line))
	{
		line.replace
	}
	
}
